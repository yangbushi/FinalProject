<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <!-- delete an item from the saved article list -->
    <Button
        android:id="@+id/article_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/article_delete"
        android:focusable="false"
        android:focusableInTouchMode="false"/>

    <!-- a title from the saved article list -->
    <TextView
        android:id="@+id/articleRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18dp"
        android:layout_toRightOf="@id/article_delete"
        android:layout_alignParentBottom="true"
        android:textColor="@color/colorAccent" />
</RelativeLayout>